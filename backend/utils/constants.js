const paginationDefaultPageSize = 10;
const paginationDefaultFullPageSize = Number.MAX_SAFE_INTEGER;
const paginationDefaultPage = 1;

module.exports = {
  paginationDefaultPageSize,
  paginationDefaultPage,
  paginationDefaultFullPageSize
};
